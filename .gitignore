# Private working notes. Names local paths and origin repos freely, which the
# checked-in docs must never do — so this must never be committed.
CLAUDE.local.md

# Local tool permissions, which record absolute paths to other repositories on
# this machine.
.claude/settings.local.json

/hsched
/bin/
/dist/
coverage.out
